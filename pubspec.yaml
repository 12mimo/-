name: xlfz
description: "一个心理相关的软件"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  provider: ^6.1.2
  shrine_images: ^2.0.2
  percent_indicator: ^4.2.3
  fl_chart: ^0.69.0

dev_dependencies:
  pedantic: ^1.11.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
